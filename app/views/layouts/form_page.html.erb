<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="South Carolina Christian Chamber of Commerce">
    <meta name="author" content="">
    <!-- <%= yield(:meta_tags) %> -->
    <link href="/favicon.ico" rel="shortcut icon" type="image/x-icon"></link>
    <%= csrf_meta_tags %>
    <title>Sc-c3</title>

    <link href="//fonts.googleapis.com/css?family=Open+Sans:400,300,600,700&subset=all" rel="stylesheet" type="text/css" />
    <link href="//fonts.googleapis.com/css?family=Lato:100,300,400,700,900,100italic,300italic,400italic,700italic,900italic" rel="stylesheet" type="text/css" />

    <!-- CSS -->
    <%= stylesheet_link_tag  'form_page', media: 'all' %>

  </head>

  <body>

    <div id="wrapper">
      <%= render 'partials/navbar' %>
      <% flash.each do |name, msg| %>
          <%= content_tag(:div,  msg.kind_of?(Array) ? msg.join(', ') : msg, class: "alert alert-info") if msg.length > 0%>
      <% end %>
      <%= yield %>
      <%= render 'partials/footer' %>
   </div>


    <script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA3gWMZ1YD5RjonFonagnfESkyBFwNMNI4&libraries=places"></script>

    <%= javascript_include_tag "form_page.js" %>

    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js"></script>

    <script type="text/javascript">
      /*Menu-toggle*/
      $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("active");
      });
      //-- Alert Timeout
      window.setTimeout(function () {
       $(".alert-info").slideUp(1000, function () {
            $(".alert-info").hide();
        });
      }, 80000);
    </script>

    <%= yield :script %>
  </body>
</html>
